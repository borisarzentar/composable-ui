import React from 'react';
import Button from '../components/atoms/Button';

export default {
  title: 'Button',
  component: Button,
};

export const DefaultButton = () => (
  <Button
    onClick={() => {}}
  >
    Test button
  </Button>
);
